# Test Fixtures

Shared test data and utilities.

## Purpose

- Provide consistent test data
- Reduce test boilerplate
- Enable repeatable tests

## Structure

```
fixtures/
├── sip-messages.json       # Sample SIP messages
├── audio-samples/          # Test audio files
└── test-credentials.json   # Mock credentials (not real)
```
